Run these powershell commands before running any SWAIN tools

(You might need to run:
"Set-ExecutionPolicy RemoteSigned" which will require running Powershell as an admin.)

Install-Module -Name ExchangeOnlineManagement

Import-Module ExchangeOnlineManagement

 --Permissions--
You will need to have permissions inside of your organizations Security & Compliance Center
Including "eDiscovery Manager"
